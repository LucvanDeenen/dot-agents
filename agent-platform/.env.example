# Copy to .env and fill in — .env itself must stay gitignored.

POSTGRES_USER=agentplatform
POSTGRES_PASSWORD=changeme
POSTGRES_DB=agentplatform

# One-time setup on the host, not stored here:
#   claude setup-token
# then copy the resulting credentials file into the `claude-credentials`
# volume (e.g. `docker cp` into a throwaway container, or a small init step
# in install.sh) so it's mounted read-only into agent-api and, per-invocation,
# into each ephemeral agent-runner container.
