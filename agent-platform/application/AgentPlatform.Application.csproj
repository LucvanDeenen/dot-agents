<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MediatR" Version="12.*"/>
    <!-- Only for the DbSet<T>/IQueryable abstractions behind IAgentDbContext — no provider package here, that's Infrastructure's job. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.9"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\domain\AgentPlatform.Domain.csproj"/>
  </ItemGroup>

</Project>
